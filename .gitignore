# environment
.venv/
.mplcache/
__pycache__/
*.pyc

# downloaded data (DEM tiles, Hansen, OSM PBF, per-site caches) — several GB
data/

# scratch
*.part
nohup.out
.DS_Store

# GitHub-Pages site (tiles + UK explorer) lives on the gh-pages branch, not main
pages/
.deploy/

# NOTE: output/ (reports, CSVs, plots) IS committed — it is the deliverable.
